package com.kkmalysa.claimmodule.application.submission;

public enum SubmissionChannel {
    MANUAL, // agent/call center: allows for missing data
    AUTO    // self-service: don't allow for missing data
}
